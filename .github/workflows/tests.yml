# Corre la suite completa en cada push y en cada pull request.
# Gratis con GitHub Actions y da el badge verde de "tests passing" en el repo.

name: tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  pruebas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      # Node se necesita para probar el nodo de n8n que decide qué se publica
      - uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Generar el calendario ficticio
        run: python scripts/generar_calendario.py

      - name: Verificar que los workflows se construyen desde el código fuente
        run: |
          python scripts/build_workflow.py
          git diff --exit-code workflows/*.json \
            || (echo "::error::Los workflows JSON no coinciden con workflows/src/*.js. Corre: python scripts/build_workflow.py" && exit 1)

      - name: Correr los tests
        run: python -m pytest tests/ -v

      - name: Verificar que el ciclo completo funciona
        run: python scripts/simular_publicacion.py
